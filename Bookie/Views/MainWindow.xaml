<Window x:Class="Bookie.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <DockPanel>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal">
            <Button Content="BooksChanged Books" Command="{Binding AddBooksCommand}" />
            <Button Content="Get Books" Command="{Binding GetBooksCommand}" />
            <Button Content="Tile View" Command="{Binding TileViewCommand}" />
        </StackPanel>
        <DataGrid ItemsSource="{Binding Books}" />
    </DockPanel>
</Window>